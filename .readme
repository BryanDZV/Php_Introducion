¡Bien! 🚀 Usar **Git** en vez de Drive es lo más limpio y profesional. Te permite mover proyectos entre PCs sin líos de archivos bloqueados, y además aprendes lo que luego te van a pedir en trabajos.

Te guío paso a paso con un ejemplo para **Wamp**:

---

## 📂 Estructura recomendada

En tu carpeta de proyectos tendrás algo así:

```
proyecto-ejemplo/
│── index.php
│── style.css
│── script.js
│── config.php      # (aquí NO pongas contraseñas reales, usa .env)
│── .gitignore
│── database/
│    └── backup.sql # aquí guardas exportaciones de la BD
```

---

## 🛠️ Paso 1: Crear repo local

En tu PC, abre Git Bash o terminal en la carpeta de tu proyecto:

```bash
cd C:\wamp64\www\proyecto-ejemplo
git init
git add .
git commit -m "Primer commit - proyecto base"
```

---

## 🛠️ Paso 2: Crear repo en GitHub/GitLab

1. Entra en GitHub (o GitLab/Bitbucket).
2. Crea un **repositorio vacío** con el mismo nombre.
3. Copia la URL (ejemplo: `https://github.com/tuusuario/proyecto-ejemplo.git`).

Conéctalo desde tu PC:

```bash
git remote add origin https://github.com/tuusuario/proyecto-ejemplo.git
git branch -M main
git push -u origin main
```

---

## 🛠️ Paso 3: Ignorar lo que no sirve

Crea un archivo `.gitignore` en tu proyecto:

```gitignore
# Ignorar configuraciones locales de Wamp
*.log
vendor/
node_modules/
*.env
database/*.sql

# Ignorar archivos sensibles
config.local.php
```

Así solo subes el **código**, no la basura ni contraseñas.

---

## 🛠️ Paso 4: Base de datos

En lugar de copiar la carpeta `mysql`, exporta/importa cuando necesites moverla:

```bash
# Exportar
mysqldump -u root -p basedatos > database/backup.sql

# Importar en otro PC
mysql -u root -p basedatos < database/backup.sql
```

Subes el `backup.sql` a Git **solo si quieres compartir datos de prueba**.

---

## 🛠️ Paso 5: Usar en otro PC

En el otro ordenador (ya con Wamp instalado):

```bash
cd C:\wamp64\www\
git clone https://github.com/tuusuario/proyecto-ejemplo.git
```

Luego importas la base de datos con el `.sql`.

---

👉 Con esto, en cualquier PC con Wamp tendrás el mismo proyecto. Solo necesitas:

* **Clonar** el repo.
* **Importar** la base de datos.
* Ajustar las configuraciones locales si cambian (ej: `config.php`).

---

¿Quieres que te prepare un **paquete listo con `.gitignore` + `backup.sql` de ejemplo + `config.php` con `.env`** para que copies/pegues y tengas la plantilla perfecta?
